name: Post From Issue

on:
  issues:
    types: [closed]

jobs:
  post-from-issue:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
      pull-requests: write

    steps:
      - name: Inspect Environment
        run: |
          set
          which m4
          which imagemagick
